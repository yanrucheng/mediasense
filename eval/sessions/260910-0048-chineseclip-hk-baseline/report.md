---
title: "ChineseCLIP 香港素材编码基线与业务评测待办"
service_version: "MediaSense 0.8.0 installed encoder"
date: 2026-09-10
environment: "local macOS; CPU; isolated evaluation process"
model_id: "OFA-Sys/chinese-clip-vit-huge-patch14@503e16b560aff94c1922f13a86a7693d36957a4f"
dataset_version: "ai-album-hk-representative-v1"
purpose: "固定素材和 Hierarchical 方法，记录真实编码速度、内存并交付人工分类预览"
baseline_ref: null
---

技术执行：**旧编码记录保留；后继业务基线的实现、执行与技术验证已完成。**
后继结果见[业务基线报告](../260910-1330-chineseclip-business-baseline/report.md)；视频抽帧及关键帧排序能力的限制已单列。
用户已确认复用当前 MediaSense 业务算法的方向，无需再次确认算法选择。
分类质量：**待人工验收**。本会话的实测保留为编码基线，还不是完整的业务基线。

人工比较结论：待定（更好／相当／更差／待定）。

简短意见：待填写。

原次实验的配置见 [config.json](config.json)，操作方法统一维护在
[模型评测 runbook](../../../readme/model-evaluation.md)。API endpoint：无，完全本地。
原始媒体、帧、向量、预览与日志均为 local-only，不进入 Git。

旧 config 与编码摘要中的“等待确认”保留为当时执行快照；当前决定及后续完成状态
由本报告交代。已确认的业务方向需要新配方和后继会话，不能靠修改旧状态字段完成接入。

## 已取得的真实结果

[编码摘要](metrics/encoding.json)记录本次观察及代码哈希；
[验证记录](metrics/validation.json)记录输入对应和单张／批量复核。

| 指标 | 实测 |
| --- | ---: |
| 源媒体 | 2,134 |
| 计划图片／帧输入 | 2,700 |
| 成功编码 | 2,697：1,851 张图片、846 个帧输入 |
| 已记录抽帧失败 | 3，均来自同一个故意损坏视频 |
| 未交代／遗漏 | 0 |
| 模型加载 | 28.604990 s |
| 编码时间 | 1,147.785283 s |
| 编码速度 | 2.349743 输入/s |
| 峰值编码进程 RSS | 4,080,992,256 bytes = 3.800720 GiB，不含子进程 |

环境实测为 Apple M5 Pro、48 GiB 统一内存、macOS 26.4.1（25E253）。
沿用已安装的 CPU / float32 / batch 4 配置，2 个 CPU 线程、nice 10；
设备可用性以对应运行记录为准，本次设备选择固定为 CPU。计时包含读取、预处理、计算、
结果取回和同步，不含抽帧、校验、向量写盘、分类与预览。没有向量缓存命中或不计时预热。

当前可打开[输入核对页](outputs/baseline/input-preview/index.html)。它有 2,697 张卡片，
明确标记“尚未分类”，不能当作 Hierarchical 结果。所有缩略图可解码，5,394 个源／输入
链接均指向存在的本地文件，无外部页面资源；已查看抽样联系表。部分图片保留未转置的
EXIF 方向，预览与本次编码输入一致。自动 headless Chrome 在此执行环境中以 -6 退出，
未取得浏览器截图，因此不声称完成浏览器交互验收。

11 项针对性测试、ruff 和改动范围的 diff 检查通过。重新编码三个不同输入的单张和
批量形式，对照基线的最大绝对分量差不超过 2.39e-7；这是对应关系验证，不是质量评分。
这 6 次额外验证编码在独立进程中完成，不混入上述性能指标。重复使用已存在的输出目录
会在模型加载前被拒绝。

本次实际执行命令（该输出已存在；重跑请用新目录）：

```bash
rtk proxy env PYTHONDONTWRITEBYTECODE=1 /Users/chengyanru/.local/share/uv/tools/mediasense/bin/python eval/shared/model_evaluation.py encode \
  --config eval/sessions/260910-0048-chineseclip-hk-baseline/config.json \
  --inputs eval/sessions/260910-0048-chineseclip-hk-baseline/outputs/prepared/inputs.json \
  --output eval/sessions/260910-0048-chineseclip-hk-baseline/outputs/baseline
```

## 已确认的业务评测范围

2026-09-10 用户确认：主评测复用当前 MediaSense 的时序视觉分组、组代表和视频
关键帧选择，评价 embedding 在真实证据压缩用途中的效果。只保留人工视觉判断、
内存和速度三个方向。算法选择已定；业务适配、执行和质量验收分别完成。

业务算法从 [compression 入口](../../../src/mediasense/precheck/compression.py)复用。
`build_adaptive_groups(content_based_boundaries=True)` 提供相邻边界与组首比较，
`select_embedding_representative` 提供现有 top-half 相似度代表规则。
素材初始分组、rendition 和视频候选帧须对齐实际生产调用；不采用旧 AI Album 的
分类结果或预计算代表，也不把所有视频帧作为独立源媒体与照片混排。

后续改动限于 `eval/shared/`、新评测会话及此 runbook。生产模型、主体架构和阶段
合约不在范围内。历史 LinearCluster 候选移植不再是主评测，不必建立另一套对照平台。

## 后续 TODO 与完成标准

本节是此次后续工作的唯一清单；共用操作方法仍由 runbook 维护。按 T1–T7 顺序推进，
T1–T7 已实施并由[后继会话](../260910-1330-chineseclip-business-baseline/report.md)提供结果、性能和验证证据；
T8 已于 2026-09-10 收到用户“我觉得挺好，没什么问题”的反馈，具体范围及限制记录在后继报告。

- [x] **T1：固定业务配方和版本。** 在 `eval/sessions/<执行时间-名称>/` 建立后继
  `config.json` 与 `report.md`，引用本会话为已有编码证据；首份业务基线的 `baseline_ref`
  留空，不把输入不同的旧编码实验当作可比质量基线。核对实际导入的 MediaSense 代码、
  有效 metadata／初始分组／rendition／视频参数，以及分组、代表选择和失败降级规则。
  当前参考源码为 `17373fa926549b671d293bfcc1635a7de664629d`，执行时记录实际版本与文件哈希。
  **完成标准：** 新配置可完整重建非模型配方，并明确 `content_based_boundaries=true`；
  时间／位置／余弦尺度、目标入口数、top_k、精确比较上限与预算都来自核实后的有效配置。
  当前参考值为 86,400 s／3,000 m／0.311、目标 200、top_k 0.5、精确上限 256、预算 65,536；
  目标 200 不承诺输出恰好 200 组，不引入旧最小权重 20 规则。旧 config、向量与性能摘要不覆盖。

- [x] **T2：准备与业务一致的固定输入。** 重新核对 fixture/verifier 和 2,134 条源媒体
  清单；从源读取本地 metadata，按现有业务规则生成初始分组、所需图像 rendition 与
  视频候选帧。核实当前生产采样规则，不默认继续用旧 10%／50%／90% PNG。
  保留源 → 初始分组 → 图片／帧的对应关系、帧位置、真实输入 SHA-256 和失败原因。
  **完成标准：** 分组可覆盖的源与明确异常源共同交代完整源清单；无未说明遗漏。
  不要求每个被代表成员都编码，但须可展开回原件；视频多帧不增加源媒体数量或权重。
  基线与候选共享模型无关的 metadata、初始分组和候选图片／帧指纹；旧缓存、名称、GPX
  和额外 RAW 不混入当前范围。已知坏视频、重复采样位置及未请求的编码均可辨认；零远程调用。

- [x] **T3：接入业务分组和代表选择。** 在现有 runner 下做薄适配，直接复用固定版本的
  `build_adaptive_groups` 和 `select_embedding_representative`；核对其输入组装与
  `_orchestrator._embeddings`／`_compression` 的实际关系。各视频在自己的候选帧内
  选择关键帧，再按业务代表路径参与分组；组代表、边界和差异较大样本由本轮向量产生。
  **完成标准：** 同一受控输入下，适配结果与直接调用业务函数的成员、代表、边界一致；
  首尾漂移、缺失 metadata／向量、坏代表的处理与所固定业务版本一致。
  无 AI Album 运行时依赖，无另一份独立演化的分组算法，无生产代码或公共合约修改。
  候选允许选出不同关键帧、代表和组，不把基线选中结果固定为候选输入。

- [x] **T4：补齐替换与比较校验。** 共用 `model_evaluation.py` 通过配置选择模型适配器，
  让 `run` 完成业务编码、分组和预览；更新仍指向旧候选的分支及错误提示。
  业务配方指纹覆盖源／候选输入、非模型信息、下游代码与参数；向量身份另含实际模型、
  权重、处理器、预处理、维度、归一化、精度与运行时。适配器继续只返回普通数值向量。
  **完成标准：** 同业务配方的另一模型配置可接入；错行、少行、无效向量或异模型向量混用
  明确失败。改变业务输入、阈值或算法时拒绝沿用旧可比基线，提示先生成对应新基线。
  模型自己的预处理变化被记录，不误判为必须与基线同一模型；不新增模型注册中心。

- [x] **T5：生成可肉眼验收的本地预览。** 复用现有 HTML，展示真实分组、所选组代表、
  边界和差异较大样本；展开可看全部源成员、视频全部候选帧及选中帧，并打开原件。
  缩略图、源媒体和实际模型输入的不同要可辨认，帧位置可核对。
  **完成标准：** 源数、编码图片／帧数、组数、失败和未处理原因分别展示；每个成员都可追溯。
  基线与候选使用相同展示方式，源可跨会话对应；能查看混组、代表不合适和关键帧遗漏。
  本地浏览器实际打开并检查展开、缩略图、原件链接；没有远程资源、历史语义命名或质量评分。
  层级只表达实际包含关系，不声称是完整凝聚式聚类树。

- [x] **T6：跑出首份业务基线。** 在新输出目录，对 T2 的全部可编码业务输入真实执行
  现有 pinned ChineseCLIP，单列加载、编码速度及峰值进程内存。先核对原模型与依赖仍可用，
  记录真实设备、精度、batch 和资源限制；不自动下载、升级或顺手更换模型／设备。
  **完成标准：** 零向量缓存命中；每个计划输入有成功或明确失败，业务分组与预览实际生成。
  计时含读取、预处理、计算、取回与同步，加载单列，RSS 子进程范围明确；报告实际处理数。
  旧向量仅在模型、处理配置与输入字节相同的情况下用于调试，记录复用来源；新输入补算
  不能与旧耗时拼成新性能。旧 2,697 输入、2.350 输入/s、3.801 GiB 只说明原次配方。

- [x] **T7：完成验证与复现交付。** 为新增适配做针对性验证，并以最终代码重建业务
  分组与预览。更新 runbook 为已执行通过的新会话命令，保留唯一入口。
  **完成标准：** 输入／向量对应、同源多帧归属、代表属于候选集合、源覆盖、无效输出拒绝、
  配方变化拒绝比较均通过；固定向量重复分组得到相同成员及代表。
  至少以小型第二适配器验证框架无关边界，不把测试替身记为真实候选质量结果。
  真实基线执行记录与重建预览均可追溯；新输出拒绝覆盖，源校验不变，无未归属遗留进程。
  Git 仅包含脚本、配置、紧凑摘要和报告，bulk 输出仍被忽略。若业务实现出现意外失败，
  记录准确错误与缺口，不改写为等待用户选择或完成；涉及产品问题交回产品线程。

- [x] **T8：人工质量验收。** 用户对照预览查看是否混入明显不同的内容、代表是否合适、
  视频选帧是否遗漏有用画面，在后继会话 `report.md` 填写日期、比较对象和简短意见。
  **完成标准：** 结论明确来自用户；无比较对象时首基线比较结论可为“待定”，不能声称更好。
  后续候选使用“更好／相当／更差／待定”。有问题保留该次结论，修订配方后开新比较，
  不反向修改历史结果；无需新增标注体系。

**技术完成门槛：** T1–T7 全部具备实际证据，新业务会话有配置、紧凑性能摘要、真实
业务预览及可执行复现命令，质量状态仍可为“待人工验收”。
**人工验收完成门槛：** T8 有用户的明确意见；技术成功不能代替质量通过，
质量较差或待定也必须如实保留。

## 原次素材与版本记录

原包位于 `/Users/chengyanru/Downloads/ai-album-hk-representative-v1`。
2026-09-10 的原包 verifier 检查中，列出的 SHA-256 全部匹配，但总大小为
3,188,639,936 bytes，超过 2 GB 上限。额外的 6,395 个文件包含后续测试副本、
缓存和预览。按原有 SHA256SUMS 的 10,607 条记录加清单自身，使用现有
`eval/shared/prepare_input.py` 重建独立的 10,608 文件副本，不改写原包，
也不采用其额外内容。本次输入仅取 media-manifest 的路径、类型与派生文件 SHA-256；
不采用历史 bundle、代表、metadata 缓存、分类名或向量。

独立副本的原包 verifier 已完整通过，逻辑大小准确匹配 1,759,421,497 bytes。
本次准备同时实际解码所有图片并探测每个视频，仅原有损坏视频失败。

源码起点：`b52535a9fa1021ba29c4e8431cc9ca5125ae4f22`。工作区另有产品线程
未提交修改；运行摘要已记录实际评测代码和已安装 encoder 的哈希。

这些素材是包中的缩小图片和稀疏代理视频，不能证明原始全码流的解码吞吐或画质。
历史输出只是比较材料，不是本次分类的正确答案。
