## 1. Skill boundary

- [x] 1.1 Add and structurally validate the `mediasense-apply` Skill.
- [x] 1.2 Add fixture-driven forward, blocker, drift, partial-failure, recovery,
  Receipt, and rewind workflow tests over the real Apply Tool boundary.

## 2. Legacy capability closure

- [x] 2.1 Record c90 code, CLI, dependency, test, and history evidence for copy
  and relative symbolic links.
- [x] 2.2 Update the active Apply contract and migration ledger with final
  `preserved`, `intentionally_changed`, `regression`, or `not_comparable`
  judgments and explicit deferred scope.

## 3. Production support boundary

- [x] 3.1 Normalize capacity, permission, read-only, disconnected/stale volume,
  and I/O errors into fail-closed Apply outcomes.
- [x] 3.2 Test ACL refusal, source/destination rebinding, storage errors, and
  journal/Receipt publication recovery using controlled fixtures.
- [x] 3.3 Add controlled large-file throughput evidence and publish the supported
  platform/filesystem matrix.

## 4. Closure

- [x] 4.1 Update README and activation evidence without broadening the active
  effect profile.
- [x] 4.2 Run Skill validation, workflow, fast, scale, fault-recovery, Ruff,
  format, lock, link, diff, and strict OpenSpec gates.
- [x] 4.3 Archive the completed OpenSpec change and create local commits without
  pushing.
