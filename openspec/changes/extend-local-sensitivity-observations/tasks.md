# 开发任务

2026-09-12 用户后续明确授权开发、合约落定及隔离验收，接受 B、D5-disabled 和 handoff 信息/失败边界。日常安装与业务 Dataset 仍不授权。实施基线及原工作区 diff 保存于 `/private/tmp/mediasense-sensitivity-260912/baseline.json` 和 `baseline.patch`。

任务按依赖排序。代码拆分、函数名和具体检查组合由实施者判断；不得借本表重开已经收敛的模型选择。验收编号来自 [acceptance](acceptance.md)，输入输出与决策来自 [design](design.md)。

## 1. 开发放行与基线

- [x] 1.1 向用户报告本计划已就绪，并记录后续明确的开始开发指示；没有该指示就停在此处。若该指示明确接受本包已展示的样例/边界，不重复询问相同事项。
- [x] 1.2 核对 D5-disabled 的开始时决定：当前按停用不纳入新 Result（包括缓存）规划，尚非单独确认；用户选择另一含义时只修对应设计、样例、delta 和 V04，再继续。
- [x] 1.3 固定实施源码基线和相关差异；确认操作不混入其他线程改动、业务 Dataset 或日常安装，保留当前 DINOv3/metadata/Plan 行为。

## 2. Handoff 与当前合约

- [x] 2.1 根据本包合成样例完成准确的 handoff/状态/失败语义审阅记录，保留 Source Item 与实际 Evidence 归属；B 方向批准不冒充精确交换值已验收。
- [x] 2.2 在当前 Run 合约采用逐模型配置、关闭、依赖不可用、缓存与资源要求；保留现有 action、Run 控制和准备范围。
- [x] 2.3 在当前 Read 属性义务/schema 采用三个具名值及状态/输入/身份/分数/坐标约束；保留 V1 历史分支和现有分页义务，形成正常与拒绝样例。
- [x] 2.4 同步唯一机器定义的发布副本，并通过相关合约结构/语义检查；不得修改旧例子含义去迁就实现。对应 V02—V04、V08—V10。

## 3. 最小模型端口与固定配方

- [x] 3.1 改造现有 SensitivityDetector/Producer 的输入对应、具名值及声明边界；执行声明可在不加载模型时读取，不新增注册服务/Tool。对应 V01、V11。
- [x] 3.2 定义三类值与纯校验：四类分布、累计依据、完整区域实例；移除新配方对通用二档阈值/标签最大值的依赖，保留旧 V1 只读解释。对应 V02、V03、V08。
- [x] 3.3 接入 Freepik 的固定 revision/权重、官方 Timm 448px、MPS FP32 和批量实现；明确属性声明、实际后端和有界失败。先用保留结果/合成端口验证结构。
- [x] 3.4 接入 640m 固定权重与 native 处理，确保真实 CPU provider；保留全部实例及整数坐标变换，不混入 320 诊断处理。先用保留结果/合成端口验证结构。
- [x] 3.5 加入不同标签的合成适配器契约测试及输入对应/非法输出反例，证明共同接口不依赖两家品牌字段。对应 V01、V11。

## 4. 配置、执行和资源

- [x] 4.1 实现逐模型配置、完整有效快照和整表覆盖；严格拒绝非法配置，旧显式配置给迁移说明，默认不启用或下载模型。对应 V04、V12。
- [x] 4.2 将模型加载/新执行的检查放到相应执行边界；配置/读取/有效缓存命中不要求可选推理库和权重存在，诊断区分配置与已验证执行。对应 V06、V08、V12。
- [x] 4.3 让两个模型分别使用 batch 4/1 及各自资源要求，覆盖模型常驻到释放的责任；保持原有资源预算与暂停/取消机制。对应 V07。
- [x] 4.4 完成缺后端、已知单项失败和未知异常的正确传播及原快照恢复，不静默跳过或把实现错误当正常等待。对应 V05。
- [x] 4.5 按真实语义依赖构建 Work 身份，验证全复用、单模型变化和纯调度变化；停用不清缓存，重启用可复用。对应 V04、V06。

## 5. 封存、读取与消费

- [x] 5.1 在 Result assembly 投影实际模型/配方/输入、三类值、已声明但未检查原因与失败；无准备输入时不制造 Evidence 身份。对应 V04、V05、V09。
- [x] 5.2 更新封存结构/语义与唯一性校验；检测错误主体、断链、重复观测、分布矛盾和非法坐标，不能为通过校验丢值。对应 V02、V03、V09。
- [x] 5.3 完成无模型读取及旧 V1 的纯只读投影；历史缺框/缺输入明确保持未知，原封存字节不变，禁止将旧标签最大值投影为实例。对应 V08。
- [x] 5.4 经现有 review/expand 和 Plan 读取边界交付完整属性、分歧、缺口与真实输入；图像叠框只消费同一输入/已证明转换，不引入模型管理 UI。对应 V09、V10。
- [x] 5.5 核对实际持久格式与旧写入者保护；需要迁移时沿现有 versioning 路径在隔离副本验证回滚，不重建或修改旧 Result。对应 V08、V12。

## 6. 定向验证及安装交付

- [x] 6.1 完成 A1 的独立语义/恢复/资源检查，并记录失败及修复范围；相关检查通过后运行默认仓库检查，保持 local_fixture/scale 的既有边界。
- [x] 6.2 在获准的隔离模型环境按 A2 的五个 development 输入执行配方一致性验证；原输入和结果只读，超出容差先定位，不重评模型或运行留出集。
- [x] 6.3 在唯一 installation runbook 中落实依赖、固定资产、本地配置、逐模型诊断和历史读取说明，并同步 packaged Skill 内的 runbook 快照及必要消费指导。
- [x] 6.4 按 runbook 固定源码、Python/extras/依赖约束和 wheel 身份；验证包内文件/契约/Skill 一致性，生产不依赖 Downloads/eval 代码或临时模型路径。
- [x] 6.5 完成 A3 的实际隔离 CLI/MCP→Run→Result→Read/Plan 路径、单模型/双模型/全关闭、缺依赖恢复、后继复用和无模型历史读取；确认不是 checkout 注入造成的假通过。
- [x] 6.6 把 V01—V12 和安装验证的真实结果、调用范围、证据身份及未认证边界写入本包验收记录，相关迁移判断回填既有台账；满足后才报告实现完成。

## 7. 日常安装的独立边界

- [ ] 7.1 若用户另行授权切换日常环境，先按 runbook 完成可复核的精确安装/备份/回滚方案，核对既有未终结 Run；当前规划及后续隔离开发不自动授权此步骤。
- [ ] 7.2 在对应授权内切换并验证实际 CLI、Skills、注册 Host 和新会话；未验证的层级保持未完成，不由隔离 wheel 通过推断日常已接通。

## 用户独立复核后的补修

- [x] R1 封存与Read共用真实输入关联/来源/尺寸校验，覆盖三个内存反例。
- [x] R2 仅转换已识别加载前提；未知加载/探针异常原样暴露，验证Run失败而非正常阻塞。
- [x] R3 通过现有Run/Read交付冻结预算、批次及去重汇总；验证分页、复用、未知历史和实际隔离MCP。

补修自检证据已交付，整体验收等待用户独立复核；不自动进入第7节日常部署。
